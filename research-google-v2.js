/* MB360 Research private connectors · Google + Meta discovery, selection and sync */
(() => {
  const SERVICES={
    google_business_profile:{label:'Google Business Profile',icon:'📍',family:'google'},
    google_search_console:{label:'Google Search Console',icon:'🔎',family:'google'},
    google_analytics_4:{label:'Google Analytics 4',icon:'📊',family:'google'},
    meta_instagram:{label:'Meta / Instagram',icon:'📸',family:'meta'}
  };
  let S={role:'',connectors:[],resources:[],sources:[],runId:null};
  const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const canEdit=()=>['admin','operator'].includes(S.role);
  const statusLabel=v=>({active:'Conectado',not_connected:'No conectado',awaiting_access:'Esperando acceso',paused:'Pausado',error:'Error',expired:'Vencido',revoked:'Revocado'})[v]||v||'No conectado';
  const edgeFor=p=>SERVICES[p]?.family==='meta'?'mb360-research-meta-sync':'mb360-research-google-sync';

  async function session(){const {data:{session}}=await sb.auth.getSession();S.role=session?.user?.app_metadata?.mb360_role||'';return session;}
  async function edge(slug,body){const s=await session();if(!s?.access_token)throw new Error('Sesión MB360 no disponible.');const r=await fetch(`${SUPABASE_URL}/functions/v1/${slug}`,{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${s.access_token}`,'apikey':SUPABASE_KEY},body:JSON.stringify(body)});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d?.message||d?.error||`HTTP ${r.status}`);return d;}
  async function currentRunId(){if(typeof window.rsv2CurrentRunId==='function'){const x=window.rsv2CurrentRunId();if(x)return x;}const {data}=await sb.from('research_runs').select('id').eq('client_id',activeWorkspaceClientId).order('created_at',{ascending:false}).limit(1);return data?.[0]?.id||null;}
  async function load(){await session();S.runId=await currentRunId();if(!activeWorkspaceClientId){S.connectors=[];S.resources=[];S.sources=[];return;}const providers=Object.keys(SERVICES),[c,r,s]=await Promise.all([sb.from('research_connectors').select('id,provider,display_name,status,config,last_success_at,last_error_at,last_error_message').eq('client_id',activeWorkspaceClientId).in('provider',providers),sb.from('research_connector_resources').select('*').eq('client_id',activeWorkspaceClientId).in('provider',providers).order('display_name'),S.runId?sb.from('research_sources').select('id,connector_id,source_ref,source_name,source_category,refresh_state,freshness_status,retrieved_at,last_refresh_error_message').eq('client_id',activeWorkspaceClientId).eq('research_run_id',S.runId):Promise.resolve({data:[],error:null})]);if(c.error)throw c.error;if(r.error)throw r.error;if(s.error)throw s.error;S.connectors=c.data||[];S.resources=r.data||[];S.sources=s.data||[];}
  function connector(p){return S.connectors.find(x=>x.provider===p)||null;}
  function serviceRow(p){const def=SERVICES[p],c=connector(p),active=c?.status==='active',resources=c?S.resources.filter(x=>x.connector_id===c.id):[],selected=resources.filter(x=>x.selected),account=c?.config?.account_label;return `<div class="rsv2-item"><div class="rsv2-item-top"><div><h3>${def.icon} ${esc(def.label)}</h3><div class="rsv2-meta">${active&&account?esc(account)+' · ':''}${esc(statusLabel(c?.status))}${selected.length?' · '+selected.length+' recurso(s) activo(s)':''}</div></div><span class="rsv2-badge ${active?'current':c?.status==='error'||c?.status==='expired'?'critical':'medium'}">${esc(statusLabel(c?.status))}</span></div>${c?.last_error_message?`<div class="rsv2-note">⚠️ ${esc(c.last_error_message)}</div>`:''}${selected.length?`<div class="rsv2-copy"><b>Usando:</b> ${selected.map(x=>esc(x.display_name)).join(' · ')}</div>`:''}${canEdit()&&active?`<div class="rsv2-actions"><button class="rsv2-action" onclick="rspDiscover('${c.id}','${p}')">Detectar recursos</button>${resources.length?`<button class="rsv2-action primary" onclick="rspConfigure('${c.id}','${p}')">Elegir recursos</button>`:''}<button class="rsv2-action" onclick="rspDisconnect('${c.id}','${def.family}')">Desconectar</button></div>`:''}</div>`;}
  async function renderCard(){if(currentTool!=='research'||!activeWorkspaceClientId)return;const host=document.querySelector('.rsv2');if(!host)return;try{await load();}catch(e){console.error('Private connectors',e);return;}let card=document.getElementById('rsv2PrivateConnectors');if(!card){card=document.createElement('div');card.id='rsv2PrivateConnectors';card.className='card';const first=host.querySelector(':scope > .card');if(first)first.insertAdjacentElement('afterend',card);else host.prepend(card);}const google=S.connectors.some(x=>SERVICES[x.provider]?.family==='google'&&x.status==='active'),meta=connector('meta_instagram')?.status==='active';card.innerHTML=`<div class="rsv2-panel-head"><div><div class="section-title">Conexiones privadas</div><div class="hint" style="margin:0">Datos autorizados por el cliente. Los tokens permanecen en Supabase Vault y nunca se exponen al navegador.</div></div>${canEdit()?`<div class="rsv2-actions"><button class="rsv2-action primary" onclick="rspConnectGoogle()">${google?'Reconectar Google':'Conectar Google'}</button><button class="rsv2-action primary" onclick="rspConnectMeta()">${meta?'Reconectar Meta':'Conectar Meta'}</button></div>`:''}</div>${!S.runId?'<div class="rsv2-note">Crea o abre una investigación antes de seleccionar recursos privados.</div>':''}<div class="rsv2-list">${Object.keys(SERVICES).map(serviceRow).join('')}</div>`;injectSyncButtons();}
  async function connectGoogle(){try{const d=await edge('mb360-research-oauth',{action:'start',provider:'google',client_id:activeWorkspaceClientId,connectors:Object.keys(SERVICES).filter(x=>SERVICES[x].family==='google'),return_url:location.origin+location.pathname});if(!d.authorization_url)throw new Error('No se recibió URL de autorización.');location.assign(d.authorization_url);}catch(e){console.error(e);toast('Google: '+(e.message||'no se pudo iniciar'));}}
  async function connectMeta(){try{const d=await edge('mb360-research-oauth',{action:'start',provider:'meta',client_id:activeWorkspaceClientId,connectors:['meta_instagram'],return_url:location.origin+location.pathname});if(!d.authorization_url)throw new Error('No se recibió URL de autorización.');location.assign(d.authorization_url);}catch(e){console.error(e);toast('Meta: '+(e.message||'no se pudo iniciar'));}}
  async function disconnect(id,family){try{if(!confirm(`¿Desconectar ${family==='meta'?'Meta / Instagram':'este servicio de Google'} del cliente activo?`))return;await edge('mb360-research-oauth',{action:'disconnect',connector_id:id});toast('Servicio desconectado ✓');await renderCard();}catch(e){console.error(e);toast('Error: '+e.message);}}
  async function discover(id,p){try{toast('Detectando recursos…');const d=await edge(edgeFor(p),{action:'discover',connector_id:id});toast(`${d.count||0} recurso(s) detectado(s) ✓`);await load();openConfig(id,p);}catch(e){console.error(e);toast(`${SERVICES[p]?.family==='meta'?'Meta':'Google'}: ${e.message}`);}}
  function dialog(html){document.getElementById('rspDialog')?.remove();const d=document.createElement('div');d.id='rspDialog';d.className='rsv2-dialog';d.innerHTML=`<div class="rsv2-dialog-card">${html}</div>`;document.body.appendChild(d);}
  function close(){document.getElementById('rspDialog')?.remove();}
  async function configureOpen(id,p){try{await load();openConfig(id,p);}catch(e){toast('Error: '+e.message);}}
  function openConfig(id,p){const c=S.connectors.find(x=>x.id===id),rows=S.resources.filter(x=>x.connector_id===id);if(!c)return;dialog(`<div class="rsv2-dialog-head"><h2>${esc(c.display_name)}</h2><button class="rsv2-close" onclick="rspClose()">×</button></div><div class="rsv2-note">Selecciona únicamente los recursos que pertenecen a este cliente y deben alimentar la investigación activa.</div>${rows.length?`<div class="rsv2-list">${rows.map(r=>`<label class="rsv2-item" style="display:flex;gap:12px;align-items:flex-start;cursor:pointer"><input type="checkbox" name="rspResource" value="${esc(r.resource_ref)}" ${r.selected?'checked':''} style="margin-top:5px;transform:scale(1.25)"><div><h3>${esc(r.display_name)}</h3><div class="rsv2-meta">${esc(r.resource_type||r.provider)} · ${esc(r.resource_ref)}</div></div></label>`).join('')}</div>`:'<div class="rsv2-empty">No hay recursos detectados. Cierra y usa “Detectar recursos”.</div>'}<div class="rsv2-actions">${rows.length?`<button class="rsv2-action primary" onclick="rspSaveConfig('${id}','${p}')">Guardar selección</button>`:''}<button class="rsv2-action" onclick="rspClose()">Cancelar</button></div>`);}
  async function saveConfig(id,p){try{if(!S.runId)throw new Error('No hay investigación activa.');const refs=[...document.querySelectorAll('input[name=rspResource]:checked')].map(x=>x.value);await edge(edgeFor(p),{action:'configure',connector_id:id,research_run_id:S.runId,resource_refs:refs});close();toast('Recursos vinculados ✓');if(typeof window.renderResearch==='function')await window.renderResearch(document.getElementById('main'));setTimeout(renderCard,30);}catch(e){console.error(e);toast('Error: '+e.message);}}
  async function syncSource(id,p){try{toast(`Sincronizando ${SERVICES[p]?.family==='meta'?'Meta':'Google'}…`);const d=await edge(edgeFor(p),{action:'sync',source_id:id,trigger_type:'manual'}),r=d?.results?.[0];if(r?.status==='failed')throw new Error(r.error||r.error_code);toast(r?.changed?'Datos actualizados · hay cambios ✓':'Datos actualizados ✓');if(typeof window.renderResearch==='function')await window.renderResearch(document.getElementById('main'));setTimeout(renderCard,30);}catch(e){console.error(e);toast('Error: '+e.message);}}
  function injectSyncButtons(){if(currentTool!=='research')return;for(const src of S.sources){if(!src.connector_id||!src.source_ref)continue;const c=S.connectors.find(x=>x.id===src.connector_id);if(!c||!SERVICES[c.provider])continue;const cards=[...document.querySelectorAll('.rsv2-item')],card=cards.find(x=>x.textContent?.includes(src.source_name));if(card&&!card.querySelector(`[data-rsp-sync="${src.id}"]`)){const actions=document.createElement('div');actions.className='rsv2-actions';actions.innerHTML=`<button class="rsv2-action primary" data-rsp-sync="${src.id}" onclick="rspSyncSource('${src.id}','${c.provider}')">Sincronizar ${SERVICES[c.provider].family==='meta'?'Meta':'Google'}</button>`;card.appendChild(actions);}}}
  async function refreshAll(){await renderCard();injectSyncButtons();}
  function install(){window.rspConnectGoogle=connectGoogle;window.rspConnectMeta=connectMeta;window.rspDisconnect=disconnect;window.rspDiscover=discover;window.rspConfigure=configureOpen;window.rspSaveConfig=saveConfig;window.rspClose=close;window.rspSyncSource=syncSource;window.rsgConnectGoogle=connectGoogle;const old=window.renderWorkspaceTool;if(typeof old==='function')window.renderWorkspaceTool=function(m){const r=old(m);Promise.resolve(r).finally(()=>setTimeout(refreshAll,20));return r;};const oldTab=window.rsv2Tab;if(typeof oldTab==='function')window.rsv2Tab=function(v){const r=oldTab(v);Promise.resolve(r).finally(()=>setTimeout(refreshAll,20));return r;};const oldRun=window.rsv2OpenRun;if(typeof oldRun==='function')window.rsv2OpenRun=async function(id){const r=await oldRun(id);setTimeout(refreshAll,20);return r;};window.addEventListener('mb360-oauth-return',()=>setTimeout(refreshAll,50));setTimeout(refreshAll,30);}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();
})();
